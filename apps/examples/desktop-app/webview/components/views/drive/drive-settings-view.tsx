"use client";

import { DriveSectionPlaceholder } from "./drive-section-placeholder";

/** Placeholder — replaced by the real settings surface. */
export function DriveSettingsView() {
	return <DriveSectionPlaceholder section="settings" />;
}
